import ToxinSliderPresenter from '../presenter.ts';

describe('ToxinSliderPresenter', function() {
    it('должен быть объявлен', function() {
        expect(ToxinSliderPresenter).toBeDefined();
    });
});